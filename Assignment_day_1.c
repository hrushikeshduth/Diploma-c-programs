#include <stdio.h>
int main()
{
float a,b,product;
//herre i declared 3 varibles a b will be multolied the value of ab will be stored in product
printf("enter the values");
scanf("%f %f",&a,&b);
product=a*b;
printf("the product is %.3f\n",product);
return 0;
}
