#include <stdio.h>

int main() {

  int i;

for(i=1;i<=10;i++)
//here we are running for loop its syntax is for(value;intilization;++or--)
{
  // now loop will run 10 times as the counter like first loop prints 1secind prints 2 thrid 3 so on till it reaches 11 it stops bevause it dosent meet condition 


printf("%d\n",i);
  
}
  
  return 0;
  
  }

